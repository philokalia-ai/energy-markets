# Euphemia input model — minimal inference dependencies (pinned).
# Install into a fresh venv, then run examples/predict_gr_tomorrow.py.
lightgbm==4.6.0
pandas==2.2.3
numpy==2.1.3
pyarrow==23.0.1
requests==2.33.0
