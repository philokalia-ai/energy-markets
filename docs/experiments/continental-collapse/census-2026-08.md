# Continental collapse census (2026-08-10, coupled-path)

Owner-approved follow-up of the GR surplus-quantity NO-SHIP: diagnose the
continental floor group's collapse residual ON the coupled path (the recorded
methodology lesson). All measurements against the fresh coupled year baseline
`trmk_base_fy` (2025-08..2026-07) + settled; anatomy on captured full books
(`books_dir` pipeline capture, 12 worst days, label `ccensus_anatomy`).

## Step 1 — where the floor stands in deep collapses (settled ≤ −50)

| zone | n | settled med | model med | at floor (−20) | stuck above |
|---|---|---|---|---|---|
| DE_LU | 44 | −78 | −20 | 77% | 23% |
| PL | 38 | −120 | −20 | 76% | 24% |
| BE | 34 | −89 | +3 | 38% | 62% |
| CZ | 33 | −102 | +4 | 42% | 58% |
| FR | 30 | −106 | +1 | **0%** | 100% |
| CH | 43 | −100 | +2 | **0%** | 100% |

## Step 2 — gate coverage in those hours (θ=0.4 solar share)

- **FR: 0% pass** — solar share med 0.33, p25 0.31, clustered just under the
  threshold; wind adds nothing. Pure gate-miss; a zonal θ≈0.3 (one nameable
  parameter) would cover nearly all of it. Mild-negative hours: only 3% pass.
- DE_LU 100%, PL 97%, BE 91% pass — their problem is floor DEPTH (−20 vs
  settled −78..−120).
- CH 65%, CZ 61% pass yet the price doesn't move — the gate fires but the
  MARGIN doesn't bind: a structural wall above the floor. PL shows the same in
  mild-negative hours (84% pass, model med +47).

## Step 3 — anatomy of the wall (110 stuck zone-hours, captured books)

Median MW that must clear above €0 (deficit) and the blocks composing the
wall between 0 and the stuck price:

- **CH** (deficit ~3,100 MW): dominated by **water_value_anchored** (~5,900 MW
  when present) — reservoir hydro priced at its anchored water value holds
  the price. Missing physics: in real surplus hours Swiss (and neighboring)
  **pumped storage pumps** — surplus absorption as DEMAND — while our books
  model pumped storage as supply-only (`FLEXIBLE_FUEL_TYPES`, no pumping leg).
- **CZ** (deficit ~2,000 MW): a thermal wall — `srmc_base` (~1,100) +
  `must_run_rest` (~900): the coupled twin of the GR cycling story (units
  that in reality ride through the valley near zero are priced at SRMC /
  half-SRMC).
- **PL** (deficit ~1,300 MW): mixed — `water_value_gas_anchored` (~950) +
  `must_run_rest` (~460) + `boundary_import` (UA book supply, ~160).

## Candidate lever map (for the prereg — gates to be frozen BEFORE any scored run)

1. **Zonal θ for FR** (gate coverage): θ_FR ≈ 0.3, one declared parameter.
2. **Floor depth** (DE_LU/PL): demand-scaled or two-tier floor below −20 in
   deep regime (the cv31 postmortem's named follow-up).
3. **Surplus pumping demand** (CH, likely AT/DE too): regime-gated
   price-taker DEMAND up to the zone's demonstrated pumping capability
   (trailing observed pumping — ex-ante), absorbing surplus the way the real
   system does. New mechanism; the census names it, sizing comes from
   demonstrated-capability data only.
4. **Thermal valley wall** (CZ, PL): the valley-continuation family — but
   sized on the COUPLED path this time, and with the GR lesson (selectivity,
   phantom control) as a hard constraint.

Next: prereg draft with regime-conditional primaries per lever family,
leave-one-out arms, fresh coupled baselines. No scored run before the prereg
merges.
