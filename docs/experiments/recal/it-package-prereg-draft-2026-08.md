# R2 package 1 — the IT family (DRAFT; freezes at morning ratification with the harness)

Zones: IT-NORTH, IT-CNORTH, IT-CSOUTH, IT-SOUTH, IT-Calabria, IT-Sicily,
IT-Sardinia. All Tier-1 actuals (R0 audit); the pilot family.

## Honest expectation calibration (measured tonight)

The QUARANTINED oracle-solar bound for IT (perfect solar input, year, the
IT-CSOUTH flip cell excluded): IT-SOUTH −0.31, IT-Sicily −0.28, IT-Calabria
−0.25, IT-CNORTH −0.23, IT-NORTH −0.20, IT-CSOUTH −0.06, IT-Sardinia
**+0.20** (its solar input is already balanced — candidate zone-level
opt-out). The input leg alone is therefore worth ~−0.25 family-wide; the
package's additional legs carry the rest of the hypothesis.

## Package legs (all re-MEASURED from defining data; price error only gates)

1. **Solar input**: the R1 winner per zone (ML vs TSO fc, scored vs the
   Tier-1 actuals). Sardinia opts out unless its R1 scorecard wins.
2. **RES capacity base**: registry is empty for IT RES (R0) — fleet-truthing
   denominators and must-run quantity bases move to trailing p99-of-actuals
   (the audit's proxy), a definitional data-source fix, not a tune.
3. **Import backstop re-derivation** (IT-CNORTH carries one): recompute the
   demonstrated-headroom sizing with the leg-1/leg-2 inputs in the loop —
   same formula, refreshed measurement.
4. NOTHING else moves (scarcity κ/thresholds stay; they enter a later
   package only if the leave-one-out of this one implicates them).

## Frozen evaluation (per the R-harness)

- Arms: baseline `trmk_base_fy`; treatment = the package, label `r2_it_fy`,
  same extract/pipeline; Set A {Aug25–Jan26} derive → Set B {Feb26–Jul26}
  scored once on A-pass.
- **Primary**: IT-family mean MAE improves ≥ 0.25 on Set A AND Set B; no IT
  zone worse than +0.20.
- Harness guards: envelope ±3.0/−0.05 all 39; no new non-quarantine caps;
  footprint ≤ +0.05; collapse hit/FA reported; quarantine rule applied.
