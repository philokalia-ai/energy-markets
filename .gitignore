.env
references/
data/

# Public reproducibility artifact (never commit large binaries)
data/public/
data/results.duckdb
*.duckdb
*.parquet

# Reproduction outputs (committed reference lives in results/reference/)
results/*
!results/reference/

# Daily-forecast SPA data (exported by bin/export_forecast_json.jl, uploaded
# as the forecast-data CI artifact)

# Results SPA: exported data is fetched at deploy time, never committed
web/data/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
!.env.example
.cloudflarerc

# Pan-European cold-ironing raw dataset (GFW non-commercial license, ~79MB) — not committed
pan-european-cold-ironing-data/
# Uncompressed pan-EU OPS profile (regenerable from build_eu_ops_profiles.py); commit the .gz
docs/experiments/scenario-exercises/ops_hourly_eu_floor_2023H2_2025H1.csv

# Claude Code local state (bundled skills in .claude/skills ARE tracked)
.claude/worktrees/

# cv21 confirm/guard transient price dumps (regenerable; not committed)
test/scripts/cv21_guard_*.tsv
docs/experiments/cv21-dk1-viking/out_*.tsv
scratch_tmp/
